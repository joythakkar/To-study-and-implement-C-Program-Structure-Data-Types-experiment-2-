#include <iostream>
using namespace std;
int main() {
int main(){

   int a;
   cout<<"Enter any integer:";
                  int main() {
   cout<<"Enter any boolean:";
   cin>>f;
   cout<<"Boolean = "<<f<<" and size is "<<sizeof(f)<<" bytes"<<endl;

    return 0;
}
}
}